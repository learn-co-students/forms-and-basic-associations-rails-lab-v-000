<h1>Create a Song</h1>

<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.text_field :artist_name, list: "artists_autocomplete", id: "song_artist_name"%> 
  <datalist id="artists_autocomplete"> 
    <%= Artist.all.each do |artist| %>
      <option value="<%= artist.name %>">
    <% end %> 
  </datalist> 

  <br>
  <%= f.collection_select :genre_id, Genre.all, :id, :name %> 
  <br/> 
  <p>Notes</p>
  <input name="song[note_contents][]" id="song_notes_1"/>
  <input name="song[note_contents][]" id="song_notes_2"/>
  <input name="song[note_contents][]" id="song_notes_3"/>
  <input name="song[note_contents][]" id="song_notes_4"/>

  <br>
  <%= f.submit %> 
<% end %>


