<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.text_field :title %> <!-- A text input box that sets the song's title. -->
  <%= f.text_field :artist_name %> <!-- A text input box for the artist. -->
  <%= f.collection_select :genre_id, Genre.all, :id, :name %> <!-- A selection box for genre. Users should be able to pick amongst existing genres only. -->
  <input name='song[note_contents][]' id='song_notes_1'> <!-- Several text input boxes to add notes to the song. These should have the IDs song_notes_# -->
  <input name='song[note_contents][]' id='song_notes_2'>
  <input name='song[note_contents][]' id='song_notes_3'>
  <br>

  <%= f.submit %>
<% end %>
