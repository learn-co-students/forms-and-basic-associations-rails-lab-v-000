<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label :song_artist_name%>
  <%=f.text_field :artist_name%>
  <br />
  <%= f.label :song_genre_id%>
  <%=f.collection_check_boxes :genre, Genre.all, :name, :id%>
  <br />
  <input name="song[note_contents][]">
  <input name="song[note_contents][]">
  <input name="song[note_contents][]">
  

  <%= f.submit %>
<% end %>


