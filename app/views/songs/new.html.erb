<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.text_field :song_artist_name %>

  <%= f.collection_select :genre, Genre.all, :name, :name %>
  <%= f.submit %>
<% end %> 
