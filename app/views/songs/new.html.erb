<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <p>
  <%= f.label :title %><br/>
  <%= f.text_field :title %>
  </p>

  <p>
  <%= f.label :artist_name %><br/>
  <%= f.text_field :artist_name %>
  </p>

  <p>
  <%= f.label :genre_name %><br/>
<!-- how this works - the object is the thing we want in the name value
 to pass back in the params hash, the method is the thing getting all
 the values, the value_method is what gets stuffed into the
 select option value=, and the text_method is what is displayed
 in the drop-down list -->
  <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  </p>

  <p>
  <%= f.label :notes %><br/>
  <input type="text" name="song[note_contents][]" id="song_notes_1"><br/>
  <input type="text" name="song[note_contents][]" id="song_notes_2">
  </p>

  <%= f.submit %>
<% end %>


