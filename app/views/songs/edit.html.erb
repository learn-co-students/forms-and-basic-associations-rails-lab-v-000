<h1>Editing <%= @song.title %></h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label :genre %>
  <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  <br />
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name, list: "songs_autocomplete" %>
 <datalist id="songs_autocomplete">
   <% Artist.all.each do |artist| %>
     <option value="<%= artist.name %>">
   <% end %>
 </datalist>
 <br />
 <input name='song[note_contents][]' id='song_notes_1'>
 <input name='song[note_contents][]' id='song_notes_2'>
 <input name='song[note_contents][]' id='song_notes_3'>
  <%= f.submit %>
<% end %>
