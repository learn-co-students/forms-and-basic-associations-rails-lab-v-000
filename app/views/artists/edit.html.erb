<h1>Editing <%= @artist.name %></h1>
<!-- 
<%= form_for @artist  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <h1>Editing <%= @song.title %></h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
 
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>

  <%= f.label :genre %>
  <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  <input name='song[note.contents][]' id='song_notes_1'>
  <input name='song[note.contents][]' id='song_notes_2'>
  <input name='song[note.contents][]' id='song_notes_3'>


  <%= f.submit %> -->
<% end %>
