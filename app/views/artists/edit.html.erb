<h1>Editing <%= @artist.name %></h1>

<%= form_for @artist  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />
  <input name="artist[song_ids][]">
  <input name="artist[song_ids][]">
  <input name="artist[song_ids][]">
  <%= f.submit %>
<% end %>
