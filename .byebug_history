exit
contents.each do |c|
     end
       self.notes << note
       note = Note.find_or_create_by(content: c)
contents.each do |c|
contents
self.notes
self
Note.all
Note.find_or_create_by(content: con)
con = contents.first
con = contents.strip
contents
con = contents[1]
c
c = contents[1]
i = contents[1]
contents[1]
     end
       self.notes << note
       note = Note.find_or_create_by(content: c)
   contents.each do |c|
contents
       self.notes << note
       note = Note.find_or_create_by(c)
contents.each do |c|
Note.all
Note
contents
